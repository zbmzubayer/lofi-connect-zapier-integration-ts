{
  "name": "test-zapier-integration",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "exports": "./dist/index.js",
  "scripts": {
    "clean": "rm -rf ./dist ./build",
    "build": "npm run clean && tsc",
    "test": "npm run build && vitest --run",
    "push": "npm run build && zapier push",
    "push:dev": "npm run build && zapier push --omit=dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^17.2.2",
    "zapier-platform-core": "17.7.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
